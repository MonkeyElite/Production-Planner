# Keycloak realm issuer URL (e.g., https://auth.example.com/realms/production)
KEYCLOAK_ISSUER=http://keycloak:8082/realms/production
# Keycloak client credentials used by NextAuth (must match your configured client)
KEYCLOAK_CLIENT_ID=gateway
KEYCLOAK_CLIENT_SECRET=gateway-secret
# Space-separated scopes to request from Keycloak (adjust to least privilege)
KEYCLOAK_SCOPE=openid profile email offline_access products.read products.write

# NextAuth configuration
# Use a strong, random value; do not commit real secrets.
NEXTAUTH_SECRET=super-secure-value
# The public URL for the Next.js app handling auth callbacks.
NEXTAUTH_URL=http://localhost:3000
